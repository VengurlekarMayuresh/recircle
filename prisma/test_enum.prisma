generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

enum Role {
  user
  admin
}

model User {
  id   Int  @id @default(autoincrement())
  role Role @default(user)
}
